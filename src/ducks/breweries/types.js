const prefix = '@@biertje/breweries';

const ADD = `${prefix}/ADD`;
const ADD_MULTIPLE = `${prefix}/ADD_MULTIPLE`;
const REMOVE = `${prefix}/REMOVE`;
const REMOVE_MULTIPLE = `${prefix}/REMOVE_MULTIPLE`;
const SET_CURRENT_CITY = `${prefix}/SET_CURRENT_CITY`;
const UNSET_CURRENT_CITY = `${prefix}/UNSET_CURRENT_CITY`;

export {
  ADD,
  ADD_MULTIPLE,
  REMOVE,
  REMOVE_MULTIPLE,
  SET_CURRENT_CITY,
  UNSET_CURRENT_CITY
};
